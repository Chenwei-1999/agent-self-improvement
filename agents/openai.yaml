interface:
  display_name: "Self Improvement"
  short_description: "Role-based conversation history audits for durable agent rules"
  default_prompt: "Use $self-improvement to audit conversation history with role-based scouts and propose evidence-backed operating rules."
